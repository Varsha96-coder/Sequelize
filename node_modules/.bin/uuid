#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node --max-old-space-size=10240"  "$basedir/../uuid/dist/bin/uuid" "$@"
  ret=$?
else 
  node --max-old-space-size=10240  "$basedir/../uuid/dist/bin/uuid" "$@"
  ret=$?
fi
exit $ret

